This is a script to generate data validation rules.

The input is metadata from NHS Digital, called a Technical Output Specification.

The output is a YAML file that is used by the R validate package.
